# Task 1 - Spring MVC + Thymeleaf

## How to run

```bash
mvn spring-boot:run
```

## Endpoints

- `GET /` -> plain text response
- `GET /greeting` -> Thymeleaf view (`greeting.html`) with an image (`vistula.png`)

## Screenshots (add later)

- Browser: `/`
- Browser: `/greeting`
